import 'package:flutter/material.dart';

class Desenvolvimento extends StatelessWidget {
  const Desenvolvimento({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        width: 350,
        child: Image.asset("assets/desenvolvimento.png"),
      ),
    );
  }
}